import * as UserModel from './user.model';
import * as OrderModel from './order.model';
import * as ProductModel from './product.model';

export const Model = { UserModel, OrderModel, ProductModel };
