import { NgForNumberDirective } from './ng-for-number.directive';

describe('NgForNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new NgForNumberDirective();
    expect(directive).toBeTruthy();
  });
});
